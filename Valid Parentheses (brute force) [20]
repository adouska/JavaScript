// TIME: O(n^2)
// SPACE: O(n)

isValid(s){
  while (s.includes("()")||s.includes("[]")||s.includes("{}")){
      s.replace("()","");
      s.replace("[]","");
      s.replace("{}","");
  }
  return s === "";
}
